id: no-retired-research-entrypoints
language: Rust
severity: error
message: Use typed research selection and origin-preserving operation diagnostics; these Plan 13 entry points are retired.
files:
  - crates/enrichment-core/src/request.rs
  - crates/enrichment-daemon/src/ops/**
rule:
  any:
    - kind: identifier
    - kind: type_identifier
  regex: '^(InspectDepth|store_error)$'
