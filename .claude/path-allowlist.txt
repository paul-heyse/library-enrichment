# Paths named in the agent instructions that do not exist yet.
#
# `scripts/check_agent_config.py` fails on any repository path an instruction document names
# that is not there, because a dead reference sends an agent somewhere that does not exist and
# nothing tells anyone. An entry here is a deliberate exception, and each one says when the
# path will appear so the exception can be removed rather than accumulating.
#
# One entry per line. `#` starts a comment.

# Operator track (register row R-11). `.claude/rules/` is part of the enforcement layer and is
# hook-denied to a session, so this rule file is drafted in docs/design/decisions-rule.draft.md
# and applied by the operator. Remove this line once it lands.
.claude/rules/decisions.md

# Operator track (register row R-16). `.claude/rules/process-immutable.md` lists
# `.claude/commands/` in its "what you can change freely" table. That directory was removed
# when the five workflows became skills, so every runtime can see them -- a command is visible
# to Claude Code only. The rule file is itself enforcement-layer and hook-denied, so the
# correction is drafted for the operator. Remove this line once that table is updated.
.claude/commands/
