help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
   ┌─ consumer/tests/decisions.rs:38:21
   │
38 │     let mut table = DeltaTable::new_in_memory().write([batch(&[1], &[10])]).await?;
   │                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
     carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
     source gains a field silently stops carrying it, or starts erroring, depending on the path.
     Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
     See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
   ┌─ consumer/tests/decisions.rs:44:19
   │
44 │     let written = table.clone().write([batch(&[2], &[20])]).with_save_mode(SaveMode::Append).await?;
   │                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
     carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
     source gains a field silently stops carrying it, or starts erroring, depending on the path.
     Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
     See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
   ┌─ consumer/tests/decisions.rs:59:17
   │
59 │     let table = DeltaTable::new_in_memory().write([batch(&[1, 2, 3], &[10, 20, 30])]).await?;
   │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
     carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
     source gains a field silently stops carrying it, or starts erroring, depending on the path.
     Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
     See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
   ┌─ consumer/tests/decisions.rs:85:18
   │
85 │     let source = DeltaTable::new_in_memory().write([batch(&[1, 2], &[10, 20])]).await?;
   │                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
     carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
     source gains a field silently stops carrying it, or starts erroring, depending on the path.
     Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
     See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
   ┌─ consumer/tests/decisions.rs:96:23
   │
96 │     let destination = DeltaTable::new_in_memory().write([batch(&[0], &[0])]).await?;
   │                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
     carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
     source gains a field silently stops carrying it, or starts erroring, depending on the path.
     Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
     See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:97:24
    │  
 97 │       let incompatible = destination.clone().write(Vec::<RecordBatch>::new())
    │                                              -----
    │ ╭────────────────────────^
    · │
100 │ │         .with_session_fallback_policy(SessionFallbackPolicy::RequireSessionState).await;
    │ ╰───────────────────────────────────────────────────────────────────────────────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:105:18
    │  
105 │       let result = destination.write(Vec::<RecordBatch>::new())
    │                                -----
    │ ╭──────────────────^
    · │
108 │ │         .with_session_fallback_policy(SessionFallbackPolicy::RequireSessionState).await?;
    │ ╰───────────────────────────────────────────────────────────────────────────────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:117:17
    │
117 │     let table = DeltaTable::new_in_memory().write([batch(&[1, 2, 3], &[10, 20, 30])]).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:118:16
    │  
118 │       let good = table.write([batch(&[2, 4], &[200, 400])]).with_save_mode(SaveMode::Overwrite)
    │                        -----
    │ ╭────────────────^
119 │ │         .with_replace_where("id >= 2").await?;
    │ ╰────────────────────────────────────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:122:19
    │  
122 │       let failure = good.clone().write([batch(&[0, 5], &[0, 500])])
    │                                  -----
    │ ╭───────────────────^
123 │ │         .with_save_mode(SaveMode::Overwrite).with_replace_where("id >= 2").await;
    │ ╰────────────────────────────────────────────────────────────────────────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:136:8
    │  
136 │       Ok(table.write([batch(&[1], &[10])]).with_save_mode(SaveMode::Append)
    │                -----
    │ ╭────────^
137 │ │         .with_commit_properties(CommitProperties::default().with_application_transaction(Transaction::new("baseline-app", marker)))
138 │ │         .await?)
    │ ╰──────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:142:17
    │
142 │     let table = DeltaTable::new_in_memory().write([batch(&[0], &[0])]).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:149:21
    │  
149 │       let unguarded = replayed.write([batch(&[1], &[10])]).with_save_mode(SaveMode::Append)
    │                                -----
    │ ╭─────────────────────^
150 │ │         .with_commit_properties(CommitProperties::default().with_application_transaction(Transaction::new("baseline-app", 5))).await?;
    │ ╰────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:158:17
    │  
158 │       let table = DeltaTable::new_in_memory().write([batch(&[1], &[10])])
    │                                               -----
    │ ╭─────────────────^
159 │ │         .with_configuration([("delta.enableChangeDataFeed", Some("true"))]).await?;
    │ ╰─────────────────────────────────────────────────────────────────────────────────^
    │  
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:160:17
    │
160 │     let table = table.write([batch(&[2], &[20])]).with_save_mode(SaveMode::Append).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:187:20
    │
187 │     let original = DeltaTable::new_in_memory().write([batch(&[1], &[10])]).await?;
    │                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:189:19
    │
189 │     let changed = original.clone().write([batch(&[2], &[20])]).with_save_mode(SaveMode::Overwrite).await?;
    │                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-schema-mode]: A write is awaited with no schema mode; a new column will not be added silently.
    ┌─ consumer/tests/decisions.rs:208:17
    │
208 │     let table = DeltaTable::new_in_memory().write([batch(&[1], &[10])]).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = WriteBuilder neither merges nor overwrites the schema by default. If the incoming batches
      carry a column the table does not have, the default is not "evolve" -- so a pipeline whose
      source gains a field silently stops carrying it, or starts erroring, depending on the path.
      Set the schema mode explicitly, even if you set it to the default, so the choice is visible.
      See content/catalogs/operations.md and content/topics/writing.md.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
   ┌─ consumer/tests/decisions.rs:38:21
   │
38 │     let mut table = DeltaTable::new_in_memory().write([batch(&[1], &[10])]).await?;
   │                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = Delta commits are idempotent only when the writer supplies an application transaction id.
     Without one there is nothing in the log that says "this batch was already written", so a
     scheduler that retries a write whose result it never saw appends the same rows twice --
     and the table is then wrong in a way no later read can detect.
     See content/topics/transactions.md.
     
     Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
     that call only stages data -- it commits separately through flush_and_commit, which is
     where its commit properties belong. Treat a match as a question about where the commit
     happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
   ┌─ consumer/tests/decisions.rs:44:19
   │
44 │     let written = table.clone().write([batch(&[2], &[20])]).with_save_mode(SaveMode::Append).await?;
   │                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = Delta commits are idempotent only when the writer supplies an application transaction id.
     Without one there is nothing in the log that says "this batch was already written", so a
     scheduler that retries a write whose result it never saw appends the same rows twice --
     and the table is then wrong in a way no later read can detect.
     See content/topics/transactions.md.
     
     Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
     that call only stages data -- it commits separately through flush_and_commit, which is
     where its commit properties belong. Treat a match as a question about where the commit
     happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
   ┌─ consumer/tests/decisions.rs:59:17
   │
59 │     let table = DeltaTable::new_in_memory().write([batch(&[1, 2, 3], &[10, 20, 30])]).await?;
   │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = Delta commits are idempotent only when the writer supplies an application transaction id.
     Without one there is nothing in the log that says "this batch was already written", so a
     scheduler that retries a write whose result it never saw appends the same rows twice --
     and the table is then wrong in a way no later read can detect.
     See content/topics/transactions.md.
     
     Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
     that call only stages data -- it commits separately through flush_and_commit, which is
     where its commit properties belong. Treat a match as a question about where the commit
     happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
   ┌─ consumer/tests/decisions.rs:85:18
   │
85 │     let source = DeltaTable::new_in_memory().write([batch(&[1, 2], &[10, 20])]).await?;
   │                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = Delta commits are idempotent only when the writer supplies an application transaction id.
     Without one there is nothing in the log that says "this batch was already written", so a
     scheduler that retries a write whose result it never saw appends the same rows twice --
     and the table is then wrong in a way no later read can detect.
     See content/topics/transactions.md.
     
     Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
     that call only stages data -- it commits separately through flush_and_commit, which is
     where its commit properties belong. Treat a match as a question about where the commit
     happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
   ┌─ consumer/tests/decisions.rs:96:23
   │
96 │     let destination = DeltaTable::new_in_memory().write([batch(&[0], &[0])]).await?;
   │                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   │
   = Delta commits are idempotent only when the writer supplies an application transaction id.
     Without one there is nothing in the log that says "this batch was already written", so a
     scheduler that retries a write whose result it never saw appends the same rows twice --
     and the table is then wrong in a way no later read can detect.
     See content/topics/transactions.md.
     
     Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
     that call only stages data -- it commits separately through flush_and_commit, which is
     where its commit properties belong. Treat a match as a question about where the commit
     happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:97:24
    │  
 97 │       let incompatible = destination.clone().write(Vec::<RecordBatch>::new())
    │                                              -----
    │ ╭────────────────────────^
    · │
100 │ │         .with_session_fallback_policy(SessionFallbackPolicy::RequireSessionState).await;
    │ ╰───────────────────────────────────────────────────────────────────────────────────────^
    │  
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:105:18
    │  
105 │       let result = destination.write(Vec::<RecordBatch>::new())
    │                                -----
    │ ╭──────────────────^
    · │
108 │ │         .with_session_fallback_policy(SessionFallbackPolicy::RequireSessionState).await?;
    │ ╰───────────────────────────────────────────────────────────────────────────────────────^
    │  
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:117:17
    │
117 │     let table = DeltaTable::new_in_memory().write([batch(&[1, 2, 3], &[10, 20, 30])]).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:118:16
    │  
118 │       let good = table.write([batch(&[2, 4], &[200, 400])]).with_save_mode(SaveMode::Overwrite)
    │                        -----
    │ ╭────────────────^
119 │ │         .with_replace_where("id >= 2").await?;
    │ ╰────────────────────────────────────────────^
    │  
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:122:19
    │  
122 │       let failure = good.clone().write([batch(&[0, 5], &[0, 500])])
    │                                  -----
    │ ╭───────────────────^
123 │ │         .with_save_mode(SaveMode::Overwrite).with_replace_where("id >= 2").await;
    │ ╰────────────────────────────────────────────────────────────────────────────────^
    │  
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:142:17
    │
142 │     let table = DeltaTable::new_in_memory().write([batch(&[0], &[0])]).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:158:17
    │  
158 │       let table = DeltaTable::new_in_memory().write([batch(&[1], &[10])])
    │                                               -----
    │ ╭─────────────────^
159 │ │         .with_configuration([("delta.enableChangeDataFeed", Some("true"))]).await?;
    │ ╰─────────────────────────────────────────────────────────────────────────────────^
    │  
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:160:17
    │
160 │     let table = table.write([batch(&[2], &[20])]).with_save_mode(SaveMode::Append).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:161:22
    │
161 │     let (table, _) = table.update().with_predicate("id = 1").with_update("value", "value + 1").await?;
    │                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:162:22
    │
162 │     let (table, _) = table.delete().with_predicate("id = 2").await?;
    │                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:187:20
    │
187 │     let original = DeltaTable::new_in_memory().write([batch(&[1], &[10])]).await?;
    │                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:189:19
    │
189 │     let changed = original.clone().write([batch(&[2], &[20])]).with_save_mode(SaveMode::Overwrite).await?;
    │                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

help[project-write-without-commit-properties]: A write is awaited with no commit properties; a retry can duplicate the data.
    ┌─ consumer/tests/decisions.rs:208:17
    │
208 │     let table = DeltaTable::new_in_memory().write([batch(&[1], &[10])]).await?;
    │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    │
    = Delta commits are idempotent only when the writer supplies an application transaction id.
      Without one there is nothing in the log that says "this batch was already written", so a
      scheduler that retries a write whose result it never saw appends the same rows twice --
      and the table is then wrong in a way no later read can detect.
      See content/topics/transactions.md.
      
      Precision note: this matches syntax, not types. RecordBatchWriter::write also matches, and
      that call only stages data -- it commits separately through flush_and_commit, which is
      where its commit properties belong. Treat a match as a question about where the commit
      happens, not as a finding that one is misconfigured.

