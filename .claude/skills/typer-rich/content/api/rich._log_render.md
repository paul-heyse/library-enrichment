# `rich._log_render`

Distribution: `rich`

## FormatTimeCallable

Import as `rich.console.FormatTimeCallable`  ·  defined at `rich._log_render.FormatTimeCallable`

```python
FormatTimeCallable = Callable[[datetime], Text]
```

_2 further import-site paths reach this item; they work but are not declared API. See `content/index/aliases.tsv`._

## c

`rich._log_render.c`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
c = Console()
```

## LogRender

Import as `rich.console.LogRender`  ·  defined at `rich._log_render.LogRender`

```python
class LogRender
```

_2 further import-site paths reach this item; they work but are not declared API. See `content/index/aliases.tsv`._

**Declared members (6)**

- `level_width = level_width`  _instance-attribute_
- `omit_repeated_times = omit_repeated_times`  _instance-attribute_
- `show_level = show_level`  _instance-attribute_
- `show_path = show_path`  _instance-attribute_
- `show_time = show_time`  _instance-attribute_
- `time_format = time_format`  _instance-attribute_

