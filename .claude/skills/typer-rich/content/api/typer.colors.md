# `typer.colors`

Distribution: `typer`

## BLACK

`typer.colors.BLACK`

```python
BLACK = 'black'
```

**Inferred type** (`ty`, not declared in the source): `Literal["black"]`

## BLUE

`typer.colors.BLUE`

```python
BLUE = 'blue'
```

**Inferred type** (`ty`, not declared in the source): `Literal["blue"]`

## BRIGHT_BLACK

`typer.colors.BRIGHT_BLACK`

```python
BRIGHT_BLACK = 'bright_black'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_black"]`

## BRIGHT_BLUE

`typer.colors.BRIGHT_BLUE`

```python
BRIGHT_BLUE = 'bright_blue'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_blue"]`

## BRIGHT_CYAN

`typer.colors.BRIGHT_CYAN`

```python
BRIGHT_CYAN = 'bright_cyan'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_cyan"]`

## BRIGHT_GREEN

`typer.colors.BRIGHT_GREEN`

```python
BRIGHT_GREEN = 'bright_green'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_green"]`

## BRIGHT_MAGENTA

`typer.colors.BRIGHT_MAGENTA`

```python
BRIGHT_MAGENTA = 'bright_magenta'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_magenta"]`

## BRIGHT_RED

`typer.colors.BRIGHT_RED`

```python
BRIGHT_RED = 'bright_red'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_red"]`

## BRIGHT_WHITE

`typer.colors.BRIGHT_WHITE`

```python
BRIGHT_WHITE = 'bright_white'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_white"]`

## BRIGHT_YELLOW

`typer.colors.BRIGHT_YELLOW`

```python
BRIGHT_YELLOW = 'bright_yellow'
```

**Inferred type** (`ty`, not declared in the source): `Literal["bright_yellow"]`

## CYAN

`typer.colors.CYAN`

```python
CYAN = 'cyan'
```

**Inferred type** (`ty`, not declared in the source): `Literal["cyan"]`

## GREEN

`typer.colors.GREEN`

```python
GREEN = 'green'
```

**Inferred type** (`ty`, not declared in the source): `Literal["green"]`

## MAGENTA

`typer.colors.MAGENTA`

```python
MAGENTA = 'magenta'
```

**Inferred type** (`ty`, not declared in the source): `Literal["magenta"]`

## RED

`typer.colors.RED`

```python
RED = 'red'
```

**Inferred type** (`ty`, not declared in the source): `Literal["red"]`

## RESET

`typer.colors.RESET`

```python
RESET = 'reset'
```

**Inferred type** (`ty`, not declared in the source): `Literal["reset"]`

## WHITE

`typer.colors.WHITE`

```python
WHITE = 'white'
```

**Inferred type** (`ty`, not declared in the source): `Literal["white"]`

## YELLOW

`typer.colors.YELLOW`

```python
YELLOW = 'yellow'
```

**Inferred type** (`ty`, not declared in the source): `Literal["yellow"]`

