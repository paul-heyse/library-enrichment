# Generated by build/queries.py from catalogs.json and the model.
# Do not hand-edit: a re-pin rewrites this file, and a hand-edit would be lost silently.
id: project-rich-version-attr
language: python
severity: hint
message: "`rich.__version__` does not exist; this raises AttributeError."
note: |
  Removed by the lazy-loading change in rich 14.3.4 (2026-04-11).

  Use `importlib.metadata.version("rich")`. Note that `typer.__version__` DOES still exist, which is what makes this easy to get wrong -- the two libraries differ, and the failing one fails at runtime.
metadata:
  generated: true
  source: catalogs.json
rule:
  pattern: rich.__version__
