# Generated by build/queries.py from catalogs.json and the model.
# Do not hand-edit: a re-pin rewrites this file, and a hand-edit would be lost silently.
id: project-typer-slim-install
language: python
severity: hint
message: "`typer_slim` is imported; it is a wrapper around `typer` and saves nothing."
note: |
  At 0.27.2 `typer-slim` is a shallow wrapper around `typer` and requires `rich` and `shellingham` unconditionally.

  Depending on `typer-slim` to keep Rich out of the dependency tree keeps nothing out. Depend on `typer`.
metadata:
  generated: true
  source: catalogs.json
rule:
  any:
    - pattern: import typer_slim
    - pattern: import typer_slim as $ALIAS
    - pattern: from typer_slim import $$$NAMES
