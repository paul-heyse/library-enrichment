# One emitted item, rather than any nested object inside one. Every `model-*` rule anchors on
# this so a match is a whole record and the reported range is the record's own.
id: json-record
language: json
rule:
  kind: object
  has:
    kind: pair
    has:
      field: key
      regex: '^"path"$'
