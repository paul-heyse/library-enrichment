id: project-rich-version-attr
valid:
  - |
    from importlib.metadata import version
    v = version('rich')
  - |
    import typer
    v = typer.__version__
invalid:
  - |
    import rich
    v = rich.__version__
