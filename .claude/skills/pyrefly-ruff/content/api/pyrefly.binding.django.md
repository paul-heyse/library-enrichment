# `pyrefly::binding::django`

Crate `pyrefly` · 1 public items · structured records in [`model/pyrefly.binding.django.json`](../model/pyrefly.binding.django.json)

## DjangoFieldInfo

`struct` · `pyrefly::binding::django::DjangoFieldInfo`

```rust
struct DjangoFieldInfo
```

**Fields**: `primary_key_field`, `foreign_key_like_fields`, `relation_fields`, `fields_with_choices`

**Derives**: Clone, Debug, Default

Django-specific field information detected during binding phase.

---
