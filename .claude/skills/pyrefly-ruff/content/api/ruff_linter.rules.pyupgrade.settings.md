# `ruff_linter::rules::pyupgrade::settings`

Crate `ruff_linter` · 1 public items · structured records in [`model/ruff_linter.rules.pyupgrade.settings.json`](../model/ruff_linter.rules.pyupgrade.settings.json)

## Settings

`struct` · `ruff_linter::rules::pyupgrade::settings::Settings`

```rust
struct Settings
```

**Fields**: `keep_runtime_typing`

**Implements**: `core::fmt::Display`, `ruff_cache::cache_key::CacheKey`

**Derives**: Clone, Debug, Default

**via `core::fmt::Display`**

```rust
fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result
```

**via `ruff_cache::cache_key::CacheKey`**

```rust
fn cache_key(&self, key: &mut ruff_cache::CacheKeyHasher)
```

---
