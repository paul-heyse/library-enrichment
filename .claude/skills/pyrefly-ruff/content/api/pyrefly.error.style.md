# `pyrefly::error::style`

Crate `pyrefly` · 1 public items · structured records in [`model/pyrefly.error.style.json`](../model/pyrefly.error.style.json)

## ErrorStyle

`enum` · `pyrefly::error::style::ErrorStyle`

```rust
enum ErrorStyle
```

**Variants**: `Delayed`, `Never`

**Implements**: `dupe::Dupe`

**Derives**: Clone, Copy, Debug, Eq, PartialEq, StructuralPartialEq

How should we print out errors for a particular module.

---
