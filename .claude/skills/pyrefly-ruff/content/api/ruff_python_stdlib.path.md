# `ruff_python_stdlib::path`

Crate `ruff_python_stdlib` · 1 public items · structured records in [`model/ruff_python_stdlib.path.json`](../model/ruff_python_stdlib.path.json)

## is_module_file

`function` · `ruff_python_stdlib::path::is_module_file`

```rust
fn is_module_file(path: &std::path::Path) -> bool
```

Return `true` if a [`Path`] should use the name of its parent directory as its module name.

---
