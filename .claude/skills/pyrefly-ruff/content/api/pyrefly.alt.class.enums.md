# `pyrefly::alt::class::enums`

Crate `pyrefly` · 3 public items · structured records in [`model/pyrefly.alt.class.enums.json`](../model/pyrefly.alt.class.enums.json)

## GENERATE_NEXT_VALUE

`constant` · `pyrefly::alt::class::enums::GENERATE_NEXT_VALUE`

```rust
const GENERATE_NEXT_VALUE: ruff_python_ast::name::Name = _
```

---

## VALUE

`constant` · `pyrefly::alt::class::enums::VALUE`

```rust
const VALUE: ruff_python_ast::name::Name = _
```

The `_value_` attribute in enums is reserved, and can be annotated to
indicate an explicit type restriction on enum members. Looking it up
on an enum member will give the raw value of that member.

---

## VALUE_PROP

`constant` · `pyrefly::alt::class::enums::VALUE_PROP`

```rust
const VALUE_PROP: ruff_python_ast::name::Name = _
```

The `value` attribute of an enum is a property that returns `_value_`.

---
