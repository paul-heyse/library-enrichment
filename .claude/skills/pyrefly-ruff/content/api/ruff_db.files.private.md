# `ruff_db::files::private`

Crate `ruff_db` · 1 public items · structured records in [`model/ruff_db.files.private.json`](../model/ruff_db.files.private.json)

## FileStatus

`enum` · `ruff_db::files::private::FileStatus`

```rust
enum FileStatus
```

**Variants**: `Exists`, `IsADirectory`, `NotFound`

---
