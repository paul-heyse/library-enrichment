# `ruff::commands::completions::config`

Crate `ruff` · 2 public items · structured records in [`model/ruff.commands.completions.config.json`](../model/ruff.commands.completions.config.json)

## OptionString

`struct` · `ruff::commands::completions::config::OptionString`

```rust
struct OptionString
```

Opaque type used solely to enable tab completions
for `ruff option [OPTION]` command.

---

## OptionStringParser

`struct` · `ruff::commands::completions::config::OptionStringParser`

```rust
struct OptionStringParser
```

---
