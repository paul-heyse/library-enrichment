# `ruff_formatter::printer`

Crate `ruff_formatter` · 1 public items · structured records in [`model/ruff_formatter.printer.json`](../model/ruff_formatter.printer.json)

## Printer

`struct` · `ruff_formatter::printer::Printer`

```rust
struct Printer<'a>
```

**Derives**: Debug, Default

Prints the format elements into a string

---
