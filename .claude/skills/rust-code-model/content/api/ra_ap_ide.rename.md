# `ra_ap_ide::rename`

Crate `ra_ap_ide` · 1 public items · structured records in [`model/ra_ap_ide.rename.json`](../model/ra_ap_ide.rename.json)

## RenameConfig

`struct` · `ra_ap_ide::rename::RenameConfig`

Also reachable as `ra_ap_ide::RenameConfig`

```rust
struct RenameConfig
```

**Fields**: `prefer_no_std`, `prefer_prelude`, `prefer_absolute`, `show_conflicts`

---
