id: project-ast-grep-json-space
valid:
  - "ast-grep run -p 'x' --json=stream src/"
  - "ast-grep scan --json=compact ."
  - "ast-grep run -p 'x' src/"
  - "rg --json stream ."
invalid:
  - "ast-grep run -p 'x' --json stream src/"
  - "ast-grep scan --json compact ."
