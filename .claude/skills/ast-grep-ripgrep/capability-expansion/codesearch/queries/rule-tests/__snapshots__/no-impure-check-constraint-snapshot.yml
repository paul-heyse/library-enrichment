id: no-impure-check-constraint
snapshots:
  table.add_constraint().with_constraint("labelled", "lattice_label(precision_rank) = 1"):
    labels:
    - source: table.add_constraint().with_constraint("labelled", "lattice_label(precision_rank) = 1")
      style: primary
      start: 0
      end: 87
  table.add_constraint().with_constraint("off_valid", "is_valid_offset(off)"):
    labels:
    - source: table.add_constraint().with_constraint("off_valid", "is_valid_offset(off)")
      style: primary
      start: 0
      end: 75
