id: no-unescaped-console-print
valid:
  - 'console.print("literal", markup=False)'
  - 'console.print(render.rows_table(rows, title=title))'
  - 'render.console().print(render.pattern(str(form)))'
invalid:
  - 'console.print("a [a-z] pattern")'
  - 'console.print(f"no rows for {title}")'
