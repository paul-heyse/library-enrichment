# `regex::pattern`

Crate `regex` · 1 public items · structured records in [`model/regex.pattern.json`](../model/regex.pattern.json)

## RegexSearcher

`struct` · `regex::pattern::RegexSearcher`

```rust
struct RegexSearcher<'r, 't>
```

---
