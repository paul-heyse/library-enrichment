# `ast_grep_config::rule::stop_by`

Crate `ast-grep-config` · 1 public items · structured records in [`model/ast_grep_config.rule.stop_by.json`](../model/ast_grep_config.rule.stop_by.json)

## SerializableStopBy

`enum` · `ast_grep_config::rule::stop_by::SerializableStopBy`

```rust
enum SerializableStopBy
```

**Variants**: `Neighbor`, `End`, `Rule`

---
