# `ast_grep_config::transform::rewrite`

Crate `ast-grep-config` · 1 public items · structured records in [`model/ast_grep_config.transform.rewrite.json`](../model/ast_grep_config.transform.rewrite.json)

## Rewrite

`struct` · `ast_grep_config::transform::rewrite::Rewrite`

```rust
struct Rewrite<T>
```

**Fields**: `source`, `rewriters`, `join_by`

---
