# `ast_grep_config::maybe`

Crate `ast-grep-config` · 1 public items · structured records in [`model/ast_grep_config.maybe.json`](../model/ast_grep_config.maybe.json)

## Maybe

`enum` · `ast_grep_config::maybe::Maybe`

```rust
enum Maybe<T>
```

**Variants**: `Absent`, `Present`

---
