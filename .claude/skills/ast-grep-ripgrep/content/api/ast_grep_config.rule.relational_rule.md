# `ast_grep_config::rule::relational_rule`

Crate `ast-grep-config` · 5 public items · structured records in [`model/ast_grep_config.rule.relational_rule.json`](../model/ast_grep_config.rule.relational_rule.json)

## Follows

`struct` · `ast_grep_config::rule::relational_rule::Follows`

```rust
struct Follows
```

---

## Has

`struct` · `ast_grep_config::rule::relational_rule::Has`

```rust
struct Has
```

---

## Inside

`struct` · `ast_grep_config::rule::relational_rule::Inside`

```rust
struct Inside
```

---

## Precedes

`struct` · `ast_grep_config::rule::relational_rule::Precedes`

```rust
struct Precedes
```

---

## Relation

`struct` · `ast_grep_config::rule::relational_rule::Relation`

```rust
struct Relation
```

**Fields**: `rule`, `stop_by`, `field`

---
