# `ast_grep_config::transform::parse`

Crate `ast-grep-config` · 1 public items · structured records in [`model/ast_grep_config.transform.parse.json`](../model/ast_grep_config.transform.parse.json)

## ParseTransError

`enum` · `ast_grep_config::transform::parse::ParseTransError`

```rust
enum ParseTransError
```

**Variants**: `Syntax`, `InvalidTransform`, `InvalidArg`, `RequiredArg`, `ArgValue`, `Regex`

---
