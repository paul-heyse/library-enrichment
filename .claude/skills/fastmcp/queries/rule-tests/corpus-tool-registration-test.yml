id: corpus-tool-registration
valid:
  - |
    def add(a: int, b: int) -> int:
        return a + b
  - |
    @staticmethod
    def helper() -> None:
        pass
invalid:
  - |
    @mcp.tool
    def add(a: int, b: int) -> int:
        return a + b
  - |
    @server.resource("data://config")
    def config() -> dict:
        return {}
