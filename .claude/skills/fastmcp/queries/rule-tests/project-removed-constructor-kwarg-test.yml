# Generated by build/queries.py. The invalid case is built from the same table as
# the rule, so the two cannot drift apart.
id: project-removed-constructor-kwarg
valid:
  - |
    mcp = FastMCP(name="demo", instructions="hello")
  - |
    mcp.run_http_async(host="0.0.0.0", port=8000)
invalid:
  - |
    mcp = FastMCP(name="demo", debug=True)
# upstream's replacement for `debug`: Set FASTMCP_DEBUG.
