id: corpus-client-session
valid:
  - |
    client = Client(server)
  - |
    with open("f") as handle:
        handle.read()
invalid:
  - |
    async with Client(server) as client:
        await client.ping()
