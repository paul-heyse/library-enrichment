# `fastmcp.cli.install`

Distribution: `fastmcp`

## install_app

Import as `fastmcp.cli.cli.install_app`  ·  defined at `fastmcp.cli.install.install_app`

```python
install_app = cyclopts.App(name='install', help='Install MCP servers in various clients and formats.')
```

**Inferred type** (`ty`, not declared in the source): `App`

_1 further import-site paths reach this item; they work but are not declared API. See `content/index/aliases.tsv`._

