# `fastmcp.server.auth`

Distribution: `fastmcp`

## __all__

`fastmcp.server.auth.__all__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
__all__ = ['AccessToken', 'AuthCheck', 'AuthContext', 'AuthProvider', 'DebugTokenVerifier', 'IdentityAssertion', 'JWTVerifier', 'MultiAuth', 'OAuthProvider', 'OAuthProxy', 'OIDCProxy', 'RemoteAuthProvider', 'StaticTokenVerifier', 'TokenVerifier', 'require_roles', 'require_scopes', 'restrict_tag', 'run_auth_checks']
```

## __getattr__

`fastmcp.server.auth.__getattr__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
def __getattr__(name: str) -> object
```

