# `mcp.client.auth`

Distribution: `mcp`

## __all__

`mcp.client.auth.__all__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
__all__ = ['AuthorizationCodeResult', 'OAuthClientProvider', 'OAuthFlowError', 'OAuthRegistrationError', 'OAuthTokenError', 'PKCEParameters', 'TokenStorage']
```

