# `fastmcp.client.auth`

Distribution: `fastmcp`

## __all__

`fastmcp.client.auth.__all__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
__all__ = ['BearerAuth', 'ClientCredentialsOAuthProvider', 'OAuth', 'PrivateKeyJWTOAuthProvider', 'SignedJWTParameters', 'static_assertion_provider']
```

