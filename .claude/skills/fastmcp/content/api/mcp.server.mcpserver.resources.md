# `mcp.server.mcpserver.resources`

Distribution: `mcp`

## __all__

`mcp.server.mcpserver.resources.__all__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
__all__ = ['Resource', 'TextResource', 'BinaryResource', 'FunctionResource', 'FileResource', 'HttpResource', 'DirectoryResource', 'ResourceTemplate', 'ResourceManager', 'ResourceSecurity', 'ResourceSecurityError', 'DEFAULT_RESOURCE_SECURITY']
```

