# `fastmcp._install_hints`

Distribution: `fastmcp`

## APP_SUPPORT

`fastmcp._install_hints.APP_SUPPORT`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
APP_SUPPORT = 'FastMCP app support is not installed. Install `fastmcp[apps]` or `fastmcp-slim[server,apps]`.'
```

## CLIENT_SUPPORT

`fastmcp._install_hints.CLIENT_SUPPORT`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
CLIENT_SUPPORT = 'FastMCP client support is not installed. Install `fastmcp` or `fastmcp-slim[client]`.'
```

## CLI_SUPPORT

`fastmcp._install_hints.CLI_SUPPORT`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
CLI_SUPPORT = 'FastMCP CLI support is not installed. Install `fastmcp` or `fastmcp-slim[server]`.'
```

## SERVER_SUPPORT

`fastmcp._install_hints.SERVER_SUPPORT`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
SERVER_SUPPORT = 'FastMCP server support is not installed. Install `fastmcp` or `fastmcp-slim[server]`.'
```

## full_package

`fastmcp._install_hints.full_package`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
def full_package(feature: str) -> str
```

