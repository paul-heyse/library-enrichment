# `fastmcp.contrib.bulk_tool_caller.example`

Distribution: `fastmcp`

## fastmcp.contrib.bulk_tool_caller

`fastmcp.contrib.bulk_tool_caller.example.bulk_tool_caller`

```python
bulk_tool_caller = BulkToolCaller()
```

## mcp

`fastmcp.contrib.bulk_tool_caller.example.mcp`

```python
mcp = FastMCP()
```

**Inferred type** (`ty`, not declared in the source): `FastMCP[Any]`

## echo_tool

`fastmcp.contrib.bulk_tool_caller.example.echo_tool`

```python
def echo_tool(text: str) -> str
```

Echo the input text


