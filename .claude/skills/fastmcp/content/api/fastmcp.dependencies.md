# `fastmcp.dependencies`

Distribution: `fastmcp`

## _MOVED_TO_TASKS

`fastmcp.dependencies._MOVED_TO_TASKS`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
_MOVED_TO_TASKS = {'CurrentDocket', 'CurrentWorker'}
```

## __all__

`fastmcp.dependencies.__all__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
__all__ = ['CallArgument', 'CurrentAccessToken', 'CurrentContext', 'CurrentFastMCP', 'CurrentHeaders', 'CurrentRequest', 'CycleError', 'Dependency', 'Depends', 'Progress', 'ProgressLike', 'Shared', 'TokenClaim']
```

## __getattr__

`fastmcp.dependencies.__getattr__`

> Not nameable: every path to this item passes through a private module. It is real and reachable at runtime, but you cannot import or annotate it.

```python
def __getattr__(name: str) -> Any
```

## CallArgument

`fastmcp.dependencies.CallArgument`

```python
CallArgument  # re-exported from uncalled_for.CallArgument
```

## CycleError

`fastmcp.dependencies.CycleError`

```python
CycleError  # re-exported from uncalled_for.CycleError
```

## Dependency

`fastmcp.dependencies.Dependency`

```python
Dependency  # re-exported from uncalled_for.Dependency
```

## Depends

`fastmcp.dependencies.Depends`

```python
Depends  # re-exported from uncalled_for.Depends
```

## Shared

`fastmcp.dependencies.Shared`

```python
Shared  # re-exported from uncalled_for.Shared
```

