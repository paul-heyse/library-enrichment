# `fastmcp.utilities.http`

Distribution: `fastmcp`

## find_available_port

Import as `fastmcp.utilities.tests.find_available_port`  ·  defined at `fastmcp.utilities.http.find_available_port`

```python
def find_available_port(host: str = '127.0.0.1') -> int
```

_3 further import-site paths reach this item; they work but are not declared API. See `content/index/aliases.tsv`._

Find an available port by letting the OS assign one.


