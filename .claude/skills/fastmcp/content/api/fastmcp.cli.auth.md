# `fastmcp.cli.auth`

Distribution: `fastmcp`

## auth_app

Import as `fastmcp.cli.cli.auth_app`  ·  defined at `fastmcp.cli.auth.auth_app`

```python
auth_app = cyclopts.App(name='auth', help='Authentication-related utilities and configuration.')
```

**Inferred type** (`ty`, not declared in the source): `App`

_1 further import-site paths reach this item; they work but are not declared API. See `content/index/aliases.tsv`._

