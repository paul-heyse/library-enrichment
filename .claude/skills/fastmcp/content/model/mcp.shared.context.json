{
 "api": "api/mcp.shared.context.md",
 "dist": "mcp",
 "items": [
  {
   "dist": "mcp",
   "doc": "api/mcp.shared.context.md#transportt",
   "inferred": "TypeVar",
   "inferred_by": "ty",
   "kind": "attribute",
   "labels": [
    "module-attribute"
   ],
   "name": "TransportT",
   "nameable": true,
   "path": "mcp.shared.context.TransportT",
   "preferred": "mcp.shared.context.TransportT",
   "signature": "TransportT = TypeVar('TransportT', bound=TransportContext, default=TransportContext, covariant=True)",
   "summary": ""
  },
  {
   "dist": "mcp",
   "doc": "api/mcp.shared.context.md#__all__",
   "kind": "attribute",
   "labels": [
    "module-attribute"
   ],
   "name": "__all__",
   "nameable": false,
   "path": "mcp.shared.context.__all__",
   "preferred": "mcp.shared.context.__all__",
   "signature": "__all__ = ['BaseContext']",
   "summary": ""
  },
  {
   "aliases": [
    "mcp.server.context.BaseContext"
   ],
   "bases": [
    "Generic[TransportT]"
   ],
   "dist": "mcp",
   "doc": "api/mcp.shared.context.md#basecontext",
   "kind": "class",
   "members": [
    {
     "kind": "attribute",
     "labels": [
      "property"
     ],
     "name": "can_send_request",
     "signature": "can_send_request: bool",
     "summary": "Whether the back-channel can currently deliver server-initiated requests."
    },
    {
     "kind": "attribute",
     "labels": [
      "property"
     ],
     "name": "cancel_requested",
     "signature": "cancel_requested: anyio.Event",
     "summary": "Set when the peer sends `notifications/cancelled` for this request."
    },
    {
     "kind": "attribute",
     "labels": [
      "property"
     ],
     "name": "meta",
     "signature": "meta: RequestParamsMeta | None",
     "summary": "The inbound request's `_meta` field, if present."
    },
    {
     "kind": "function",
     "labels": [
      "async"
     ],
     "name": "notify",
     "signature": "async def notify(self, method: str, params: Mapping[str, Any] | None, opts: CallOptions | None = None) -> None",
     "summary": "Send a notification to the peer on the back-channel."
    },
    {
     "kind": "function",
     "labels": [
      "async"
     ],
     "name": "report_progress",
     "signature": "async def report_progress(self, progress: float, total: float | None = None, message: str | None = None) -> None",
     "summary": "Report progress for this request, if the peer supplied a progress token."
    },
    {
     "kind": "function",
     "labels": [
      "async"
     ],
     "name": "send_raw_request",
     "signature": "async def send_raw_request(self, method: str, params: Mapping[str, Any] | None, opts: CallOptions | None = None) -> dict[str, Any]",
     "summary": "Send a request to the peer on the back-channel."
    },
    {
     "kind": "attribute",
     "labels": [
      "property"
     ],
     "name": "transport",
     "signature": "transport: TransportT",
     "summary": "Transport-specific metadata for this inbound request."
    }
   ],
   "name": "BaseContext",
   "nameable": true,
   "path": "mcp.shared.context.BaseContext",
   "preferred": "mcp.server.context.BaseContext",
   "signature": "class BaseContext(Generic[TransportT])",
   "summary": "Per-request context wrapping a `DispatchContext`."
  }
 ],
 "module": "mcp.shared.context"
}
