# Plan 13 removal evidence — 2026-09-15

This is a source and consumer inventory for the hard pivot, not a substitute for Plan 13's
functional exits. Historical frozen bundles and review text remain provenance. The active
research contract is 2.0; state, snapshot/projection, catalog, jobs and bundle generations have
changed together. **D01–D12 are closed** by the native replacement, final independent command
replays, actual installed agent tasks and verified production activation recorded below.

## Active ownership and replacement paths

| ID | Source evidence | Passed behavioral evidence |
|---|---|---|
| D01 | `enrichment-store/src/coverage.rs` evaluates transient requested domains against qualified retained facts. Resolve, search, overview, inspection and both comparison sides consume this assessment. Raw producer coverage remains input evidence, not an independently assembled research assessment. | Final native coverage/cold-offline fixtures passed. Installed 54.1→55.1 comparison preserves 24 changed documentation keys and separate missing release-note assessments; retained/offline scope agrees (J03/J04/J10). |
| D02 | `enrichment-core/src/wire/research.rs::ResearchSelection` supplies the default or explicit independent aspects. `request.rs` rejects unknown original fields; the adapter validates original JSON before Pydantic lowering. | Strict MCP/RPC rejection passed. All four required DataFusion 55.1.0 default inspections passed on the installed release (J01). |
| D03 | `enrichment-store/src/query.rs` selects bounded observation, relationship, navigation, document and execution pages before decoding their values. `ops/inspect.rs` records each aspect's disposition and continuation. | Independent 97-alternative traversal passed. Installed SessionContext navigation returns all 117 retained relationships once. Equal-result endpoint measurements support the selected semi join (J02/W11). |
| D04 | `enrichment-store/src/comparison.rs` reconciles flat `(key,value)` relations with `EXCEPT DISTINCT`, then indexes changed keys and selects each side's alternatives independently. No comparison group aggregate constructs complete alternative arrays. | High-variant traversal, cold comparison/recovery and all formerly overflowing comparison publication cases passed in the final 432-test native replay. Publication futures are heap-owned before polling; no stack limit was raised. Installed live comparisons and equal-result index measurements passed (J03/J05). |
| D05 | `query_failure.rs` walks concrete DataFusion/Arrow/I/O causes; `preparation.rs` supplies invariant witnesses; `ops/common.rs` and execution readiness project typed actions. The retired blanket `store_error` entry point is mechanically prohibited. | The final native run passed unknown/missing/corrupt journal, native cause/action, bounded persistent failure history, independent field requirements and invariant-witness cases (J06/J15). |
| D06 | `DeliveryDescriptor`, `JobResult`, daemon delivery and indexed result storage separate outcome from delivery location. Native execution publication still legitimately records `result_artifact_ids`; those are source/result closure identities, not the removed error-shaped overflow path. | Raw stdio and compact terminal/artifact journeys, actual Codex/Claude failed-job recovery, final Python publication/export and deployed pending completion all passed (J07/J12/J17/J19). |
| D07 | `enrichment-store/src/result.rs` writes a canonical section index once. Artifact reads seek bounded sections and calculate encoded response size; recovery opens immutable bytes read-only. | Native Unicode/direct-section/recovery, installed reads and 135 measured complete tasks, final Python export, and deployed named-section digest verification passed (J08/J17). |
| D08 | Authored `python/enrichment_mcp/presentation.py` composes generated Rust DTOs; operation schema/default/effect conformance checks cover lowering. Adapter-local offline status has its own typed variant. | Catalog, strict-input, raw-wire, focused installed and all fourteen actual installed client scenarios passed (J12/J13/J19). |
| D09 | `python/enrichment_mcp/daemon_client.py` enforces frame length/completion and RPC shape/ID with distinct connection, timeout, oversize and malformed-response diagnostics. | Fifteen real Unix peer cases passed; no generic unavailable mapping replaces an admitted native error. |
| D10 | Revision extraction records safe link omissions. Bounded Cargo input collection plus native joins/aggregates classify required and potentially relevant omitted inputs. Hostile paths and metadata still fail closed. | Archive and declared-input cases passed. Immutable source URIs now retain revision and package path correctly. The exact campaign commit produces useful partial evidence, explicit LICENSE/NOTICE link omissions and identical offline context/snapshot (J09). |
| D11 | `state.rs`, snapshot/projection contracts, catalog/jobs and bundle verifier admit only current formats. Generation-specific execution qualification state preserves earlier state without adopting it. | Old-root preservation/rejection and fresh generation qualification passed. Production now selects the verified three release binaries, adapter and worker, admits only current formats and preserves old evidence inactive (J20). |
| D12 | Product skill, generated resources, authored tool presentation, operation examples and test helpers target current selections, delivery and job results. Original historical bundles are excluded from active generation. | Public examples, generated presentation, both actual agents and deployed smoke passed. Both global registrations match the installed launch and both managed skill copies match all shipped files (J19/J20). |

All source paths above are under `crates/` unless prefixed with `python/`. Every row now has
behavioral and relevant installed-consumer evidence. Tests and production sources remain together
in the dirty Plan 13 working tree; no commit, reset or cleanup was performed.

## Source-shape guards and interpretation

`rules/no-retired-research-entrypoints.yml` rejects `InspectDepth` and the blanket `store_error`
entry point in the active request/operation modules. `rules/no-previous-research-cursors.yml`
rejects the previous cursor decoders. Their fixtures distinguish active compatibility paths
from legitimate historical names. Neither rule proves relational correctness or lifecycle safety.

The surviving `array_agg` uses in search fold qualified aliases; they are not the retired
comparison alternative accumulator. Producer-worker schema identities are separate from the
public research envelope. A textual match for `depth`, `overflow` or `result_artifact_id` alone
does not establish executable compatibility; its current consumer determines its meaning.

## Evidence binding

Independent final replays in `.dev-state/plan13-independent-audit/final/` passed **432 native,
223 Python, 12 execution, 2 live and 15 installed-client tests**. All five source-bound receipts
validate against `224dea96aca19e8482b6715f524ebe0b5948ba039e0c8fac00200aa933d72bf5`; byte-identical
copies supply the standard acceptance report. The original `just ci` and client runs with obsolete
observer assertions remain preserved as failed diagnostics. Final integration qualification uses
the corrected affected checks and independent full component replays.

The current installed candidate is `/home/paul/.local/opt/library-enrichment/plan13-3ae079f9718a`.
Its focused receipt is `.dev-state/p13-installed-live/focused-130513-receipt.json`, with unchanged
source and native hashes. The preceding candidate's 40-crate campaign and measured 135 tasks
remain attributed to that earlier installation; native binaries are identical. The successor
changes portable error presentation and launch selection. Exact results and limitations are in
[the live report](plan13-live-functional-outcomes-2026-09-15.md) and
[the measurement report](plan13-native-operation-measurements-2026-09-15.md).

Production activation and deployed status/resolve/inspection/comparison/pending/direct-read
verification passed at 13:49–13:50 UTC. All installed input hashes match, prior data/configuration
are unchanged, old daemon/adapters are stopped and no task-owned workers remain. The
[final qualification report](plan13-final-qualification-2026-09-15.md) maps all J01–J20 outcomes,
the independent acceptance audit and exact deployment receipts.
