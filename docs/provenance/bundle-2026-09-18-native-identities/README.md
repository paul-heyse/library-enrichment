# Native identity guidance provenance — 2026-09-18

Authority: accepted ADR-0047 and the authorized Plan 19 implementation. This seals the active
wire-8 product guide, canonical UUID identity domains and exact job clocks. It does not certify
installation or complete the plan's qualification gates.

The immediately preceding guide is retained byte-for-byte and verified against the existing
wire-7 bundle seal. TYPED_COMPARISON_PATHMAP.tsv relocates only that guide. All original frozen
manifests, maps and specification bytes remain untouched. The provenance gate verifies every
bundle; the runtime has only the current state and wire reader.
