# Native grants and generated resources provenance — 2026-09-18

Authority: accepted ADR-0047 and the authorized Plan 19 hard pivot. This seals the active
wire-9 product guidance. Native grant identities now carry a binary semantic domain; the
Rust resource catalog generates all five FastMCP resource bindings from operation contracts.
This records contract implementation, not installed or terminal acceptance.

The prior wire-8 guide is preserved byte-for-byte. NATIVE_IDENTITIES_PATHMAP.tsv relocates
only that guide for verification against its existing seal. All earlier bundle bytes remain
unchanged. Historical provenance is not an active compatibility reader.
