# Blueprint §7.3 budgets are measured in bytes and entries, so arithmetic on sizes is
# load-bearing; keep the cognitive-complexity bar low enough that budget code stays reviewable.
cognitive-complexity-threshold = 20
too-many-arguments-threshold = 8
# Evidence records carry provenance; a large enum variant is usually a missing Box.
enum-variant-size-threshold = 256
