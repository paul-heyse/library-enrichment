id: native-acquisition-input-owner
valid:
  - 'let mut bytes = OwnedBuffer::new(&pool, limit, "http")?;'
  - 'let mut line = Vec::new();'
  - 'blobs.read_owned(&artifact, limit, &pool)'
  - 'OwnedBytes::read_range(&mut file, start..end, &pool)?'
invalid:
  - 'let mut bytes = Vec::new();'
  - 'file.read_to_end(&mut bytes)?'
  - 'crate::leases::shared(root)?'
  - 'fn range(file: File) -> Vec<u8> { vec![] }'
