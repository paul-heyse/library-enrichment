id: no-service-phase-claim-in-adapter
valid:
  - |
      def _not_implemented(tool):
          phase = _UNIMPLEMENTED_IN[tool]
          return error(f"`{tool}` lands in phase {phase}. Call `service_status`.")
  - |
      SUMMARY = "phase 1 producers are installed"
invalid:
  - |
      def _not_implemented(tool):
          return error(f"This service is at phase 0; `{tool}` lands in phase 3.")
  - |
      NEXT = "The service is at phase 2; retry after the next release."
