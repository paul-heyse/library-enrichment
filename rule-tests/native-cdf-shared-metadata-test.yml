id: native-cdf-shared-metadata
valid:
  - 'let cache = session.runtime_env().cache_manager.get_file_metadata_cache();'
  - 'mod tests { fn probe() { let cache = DefaultCache::new(1024); } }'
invalid:
  - 'let cache = DefaultCache::new(1024 * 1024);'
  - 'let manager = CacheManager::try_new(&config)?;'
  - 'let context = SessionContext::new();'
  - 'let context = DeltaSessionContext::default();'
