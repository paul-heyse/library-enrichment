id: native-immutable-ingress-owner
valid:
  - 'let input = native_catalog::batch(&session, "input", batch)?;'
  - 'let input = native_catalog::captured_batches(&session, "input", batches)?;'
invalid:
  - 'session.read_batch(batch)?'
  - 'runtime.session().read_batches(batches)?'
