id: native-rpc-keeps-encoded-values
valid:
  - 'fn project() { JsonOutput::write(pool, bytes, encode) }'
  - 'fn external_reader() { serde_json::from_slice(bytes)?; }'
  - 'mod tests { fn research_response() { serde_json::to_value(value)?; } }'
invalid:
  - 'fn project() { serde_json::from_slice(&output) }'
  - 'fn research_response() { serde_json::to_value(envelope) }'
  - 'fn write_response() { serde_json::to_vec(response) }'
