id: native-decoder-contract
valid:
  - 'use enrichment_core::execution::rustdoc_decoder::{Request, Report};'
  - 'struct Captured { directory: Arc<PrivateDirectory> }'
  - 'mod tests { struct Report { bad: bool } }'
invalid:
  - 'struct Request { path: PathBuf, deadline: Duration }'
  - 'pub(crate) struct StreamReceipt { bytes: u64, rows: u64 }'
