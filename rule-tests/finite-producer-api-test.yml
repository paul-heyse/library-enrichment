id: finite-producer-api
valid:
  - 'pub async fn run(&self, image: &str, root: &Path, invocation: &Invocation, cancel: Arc<AtomicBool>) {}'
  - 'fn operation_from_command(command: ProducerCommand) {}'
  - 'mod tests { fn prepare_outputs(argv: &[String]) {} }'
invalid:
  - 'pub async fn run(&self, image: &str, root: &Path, argv: &[String]) {}'
  - 'pub async fn serve(&self, argv: Vec<String>) {}'
  - 'pub async fn acquire(&self) {}'
  - 'pub async fn prepare_outputs(&self) {}'
