id: native-field-rules-no-role-dispatch
valid:
  - 'let rule = field.metadata().get("enrichment.rule");'
  - 'let violations = native_schema::intrinsic_witness(frame, schema, key)?;'
invalid:
  - 'let role = field.metadata().get("enrichment.role");'
  - 'let vocabulary = role.strip_prefix("vocabulary:");'
