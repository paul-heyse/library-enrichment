id: no-mocks-in-acceptance-tiers
valid:
  - |
      from fastmcp import Client
      async def test_catalog(server):
          async with Client(server) as c:
              assert await c.list_tools()
invalid:
  - |
      from unittest.mock import AsyncMock
  - |
      import unittest.mock
