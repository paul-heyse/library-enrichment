id: native-field-reference-owner
valid:
  - 'let rule = Rule::foreign_key("snapshots", "snapshot_id");'
  - 'let references = field_admission::violations(&session, source, schema, key, namespace).await?;'
invalid:
  - 'let rules = table.references();'
  - 'pub(crate) struct ReferenceRule { field: String }'
