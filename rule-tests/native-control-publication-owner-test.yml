id: native-control-publication-owner
valid:
  - 'let admitted = admission::check(&runtime, &session, &command).await?;'
  - 'self.append_control(table, input, transactions).await?'
  - 'async fn current_selection() { base.current(&runtime, &context).await?; }'
  - 'async fn copy_immutable_catalog() { target.delta.append(table, &contract, input, transactions).await?; }'
invalid:
  - 'async fn commit_native() { self.delta.append(table, &contract, input, transactions).await?; }'
  - 'async fn commit_once() { base.current(&runtime, &context).await?; }'
  - 'async fn commit_once() { base.job_publication(&runtime, &job).await?; }'
  - 'async fn commit_once() { base.comparison_publication(&runtime, &job).await?; }'
