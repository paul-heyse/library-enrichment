id: native-object-bytes-owner
valid:
  - 'executor.spawn_io_blocking(move || read_range(file, range, pool))'
  - 'self.file_result(location, options).await'
  - 'self.inner.put_opts(location, payload, options).await'
invalid:
  - 'tokio::task::spawn_blocking(move || synchronize(path)).await'
  - 'self.inner.get_ranges(location, ranges).await'
