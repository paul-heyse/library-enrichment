id: native-captured-input-owner
valid:
  - 'fn batch() { Arc::new(AdmittedProvider::from_batch(batch)?); }'
  - 'fn mutation_refusal_fixture() { MemTable::try_new(schema, rows); }'
invalid:
  - 'fn batch() { Arc::new(datafusion::datasource::MemTable::try_new(schema, rows)?); }'
  - 'fn batch() { MemTable::try_new(schema, rows)?; }'
