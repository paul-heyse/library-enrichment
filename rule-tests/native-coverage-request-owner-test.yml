id: native-coverage-request-owner
valid:
  - 'let requested = evidence_requests(runtime, &session).await?;'
  - 'async fn fixture() { kinds.iter().copied().collect(); }'
invalid:
  - 'frame.with_param_values(params)?'
  - 'async fn assess() { kinds.iter().copied().collect(); }'
