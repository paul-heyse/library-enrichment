id: native-delta-reference-owner
valid:
  - 'struct Identity { contract_id: SchemaContractId, cohort_id: CohortId }'
  - 'native_struct! { pub struct DeltaTableRef { table_id: String => Rule::NonEmpty, contract_id: SchemaContractId => Rule::Text } }'
  - 'native_struct! { pub struct DeltaBinding { source: DeltaVersionRef => Rule::Text, cohort_id: CohortId => Rule::Text } }'
  - 'native_struct! { pub struct TableSelection { source: DeltaVersionRef => Rule::Text, row: Option<RowKey> => Rule::Text } }'
invalid:
  - 'struct Input { cohort_id: String }'
  - 'struct Input { contract_id: String }'
  - 'native_struct! { pub struct Input { contract_id: String => Rule::NonEmpty } }'
  - 'native_struct! { pub struct Input { cohort_id: String => Rule::NonEmpty } }'
  - 'native_struct! { pub struct DefinitionBinding { version: u64 => Rule::Text } }'
  - 'native_struct! { pub struct TableVersion { version: u64 => Rule::Text } }'
  - 'native_struct! { pub struct DeltaBinding { table_id: String => Rule::NonEmpty } }'
  - 'native_struct! { pub struct ResultVersion { contract_id: SchemaContractId => Rule::Text } }'
  - 'native_struct! { pub struct StorageCapture { table_uri: String => Rule::NonEmpty } }'
