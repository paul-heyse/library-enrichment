id: native-semantic-response-owner
valid:
  - 'async fn query() { let result = semantic_response_plan::lower(&runtime, capture).await?; }'
  - 'fn frame() { let message = serde_json::from_slice(bytes)?; }'
  - 'mod tests { fn range() { assert!(true); } }'
invalid:
  - 'fn normalize_answer(value: &Value) {}'
  - 'fn byte_position(document: &str, line: u32) {}'
  - 'fn query() { let value = SemanticQuery { outcome: ExecutionOutcome::Empty }; }'
